# RL Final Project - Student ID: 40301694
# Install with:  pip install -r requirements.txt
#
# Note: tkinter is used by the GUI but is NOT installable via pip.
# It ships with the official Python installer on Windows and macOS.
# On Debian/Ubuntu install it with:  sudo apt-get install python3-tk

numpy>=1.24
matplotlib>=3.7
pandas>=2.0
pytest>=7.4